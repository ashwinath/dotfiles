[user]
	name = Ashwin Chatterji
[alias]
    shame = blame
    co = checkout
    st = status
    br = branch
    fap = fetch --prune
    p = pull --ff-only
    fa = fetch --all --tags
    cp = cherry-pick
    l = log --graph --oneline --all --decorate
[core]
    excludesfile = /home/ashwin/.gitignore
# BEGIN Managed by git_ssh role

[includeIf "gitdir:/Users/achatterji/indeed/"]
    path = .gitconfig-ind
[includeIf "gitdir:/Users/achatterji/indeed/"]
    path = .gitconfig-ind
[includeIf "gitdir:/Users/achatterji/env"]
    path = .gitconfig-ind
[includeIf "gitdir:~/artifacts/"]
    path = .gitconfig-ind
[includeIf "gitdir:~/go/src/indeed.com/"]
    path = .gitconfig-ind
[includeIf "hasconfig:remote.*.url:https://code.corp.indeed.com/*/**"]
    path = .gitconfig-ind
[includeIf "hasconfig:remote.*.url:git@code.corp.indeed.com:*/**"]
    path = .gitconfig-ind
# END Managed by git_ssh role
# BEGIN Managed by shell_setup role
[include]
    path = /Users/achatterji/env/etc/git/aliases
    path = /Users/achatterji/env/etc/git/general-config

 
# END Managed by shell_setup role
